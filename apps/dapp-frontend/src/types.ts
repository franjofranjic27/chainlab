/** Top-level UI state types — see handoff "State management". */
export type Theme = "dark" | "light";

export type PageId =
  | "dashboard"
  | "wallet"
  | "swap"
  | "pool"
  | "nft"
  | "history"
  | "analytics"
  | "settings";
